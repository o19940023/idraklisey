rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Production rules for Idrak Liseyi platform
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
